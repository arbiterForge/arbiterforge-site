# arbiterforge-site

Splash page for **arbiterforge.com**.

A single self-contained `index.html` — no build step, no dependencies, no external requests.
Published to GitHub Pages from the default branch; `CNAME` binds the custom domain.

Design tokens (background, gold accent, type scale) mirror the codeArbiter documentation site
so the three properties read as one system.
